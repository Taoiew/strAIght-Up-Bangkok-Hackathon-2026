# Prompt Modules

Edit these files when adapting the starter for a new hackathon challenge:

- `system.md`: product identity, role, task behavior, domain instructions.
- `guardrails.md`: durable safety and business rules.
- `output.md`: default response style.

The agent assembles these files at runtime with values from `lib/config/product.ts`.
